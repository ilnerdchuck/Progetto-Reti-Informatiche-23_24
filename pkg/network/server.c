#include "network.h"

server* new_server(AcceptFunction a, InputFunction i, ResponseFunction r) {
    return NULL;
}
int bind_server(server* s, size_t port) { return 0; }
int listen_server(server* s) { return 0; }
void delete_server(server* s) {}
